# AI Docs System — Инструкции

> **Конфигурация:** `.ai-docs-system/config.env`  
> **Шаблоны:** `.ai-docs-system/templates/`

---

<!-- АВТОМАТИЧЕСКИ СОБРАНО ИЗ rules/ -->
<!-- Редактируйте rules/*.md и запустите install.sh update для пересборки -->

## Doc-first (ОБЯЗАТЕЛЬНО)

Перед любым ответом или изменением кода:

1. **Прочитай** `docs/README.md` — это точка входа в документацию
2. **Найди** релевантные документы в `docs/` по теме запроса
3. **Следуй** существующей документации
4. **Если документация устарела** или противоречит коду — явно скажи об этом и предложи diff на обновление

> Документация — источник правды. Код должен соответствовать документации, не наоборот.

---

## Обновление документации

При изменении кода — **всегда** предлагай обновить соответствующую документацию.

### Маппинг изменений

| Что изменилось | Где обновить документацию |
|----------------|---------------------------|
| Бизнес-логика, компоненты, хуки, страницы | `docs/features/` |
| Схема данных, модели, типы, миграции | `docs/architecture/` |
| Инфраструктура, Docker, CI/CD, деплой | `docs/infrastructure/` |

### Как обновлять

1. Определи какие файлы изменились (`git diff --name-only`)
2. По маппингу (см. `config.env`) найди соответствующую документацию
3. Предложи конкретные изменения в формате diff
4. Примени после подтверждения

### Метаданные документов

⚠️ **СТРОГИЙ WHITELIST — НЕ НАРУШАЙ:**

**ТОЛЬКО эти поля** разрешены:

```markdown
> **Status:** current | draft | legacy  
> **Last verified:** YYYY-MM-DD HH:MM  
> **Owner:** @username
```

- **Status:** `current` (актуальный), `draft` (черновик), `legacy` (устаревший)
- **Last verified:** дата и время последней проверки (формат `YYYY-MM-DD HH:MM`)
- **Owner:** ответственный за документ

⛔ **ЗАПРЕЩЕНО добавлять** другие поля:
- ❌ Priority, Tags, Version, Category, Difficulty, etc.
- ❌ Любые поля вне whitelist — **даже если кажутся логичными**

**Если нужно новое поле:**
1. ОСТАНОВИСЬ
2. СПРОСИ пользователя явно
3. НЕ добавляй самостоятельно

При обновлении документа — обнови `Last verified` на текущие дату и время.

---

## Architecture Decision Records (ADR)

При принятии **архитектурного решения** — создавай ADR.

### Когда создавать ADR

- Выбор фреймворка, библиотеки или инструмента
- Решение о паттерне проектирования
- Компромиссы между вариантами
- Изменение существующей архитектуры
- Любое решение, влияющее на несколько частей системы

### Формат

```
docs/adr/NNNN-название-решения.md
```

Где `NNNN` — последовательный номер (0001, 0002, ...).

### Структура ADR

1. **Контекст** — какая проблема решается
2. **Решение** — что решили и почему
3. **Альтернативы** — что ещё рассматривали
4. **Последствия** — что изменится после решения

Используй шаблон из `.ai-docs-system/templates/adr.md`

---

## Шорткаты

### `==` — Обновить документацию

Когда пользователь вводит **"=="**, выполни:

1. **Проверь pending updates:**
   - Прочитай `PENDING_UPDATES_LOCAL` из конфига (если файл существует)
   - Прочитай `PENDING_UPDATES_SHARED` если задан
   - Также проверь `.queue0` файлы (fallback для путей с проблемными символами)
   
   **Формат обычного файла (.queue):**
   - Поля разделены `|`: `timestamp|kind|ref|files_tab|doc_hint|note`
   - Файлы внутри `files_tab` разделены TAB (`\t`)
   
   **Формат .queue0 (NUL-separated fallback):**
   - Все поля и файлы разделены NUL (`\0`)
   - Формат: `ts\0kind\0ref\0file1\0file2\0...\0\0doc_hint\0note\0\0`
   - Двойной NUL (`\0\0`) = конец записи
   - Парсить: читать до первого `\0\0`, затем следующую запись
   - Используется когда пути содержат `|`, `\t` или `\n`
   
   - Если есть записи → покажи пользователю:
     ```
     📋 Необработанные изменения из предыдущих сессий:
     
     2026-01-22 15:30 (2 часа назад):
       • src/hooks/usePayments.ts
       • src/components/PaymentForm.tsx
       → Рекомендуется: docs/features/payments/
     
     Обработать их сейчас?
     ```
   - После обработки — **удали строку** из .queue файла (или запись из .queue0)

2. **Проверь устаревшие документы:**
   - Найди все файлы в `docs/` с полем `Last verified`
   - Если дата > `DOC_STALE_DAYS` дней назад → добавь в список
   - Если список > `DOC_STALE_MAX` → показывай топ + "ещё N документов"
   - Покажи пользователю:
     ```
     ⏰ Эти документы не проверялись >30 дней:
     
       • docs/infrastructure/DEPLOY.md — 219 дней назад
       • docs/features/auth/README.md — 52 дня назад
       (ещё 3 документа...)
     
     Пройтись по ним сейчас?
     ```
   - Если пользователь соглашается → прочитай, проверь актуальность, обнови `Last verified`

3. **Найди текущие изменения** — `git diff --name-only` (staged и unstaged)

4. **Проверь существующую документацию** — есть ли уже документы по этой теме в `docs/`

5. **Определи затронутые документы** по маппингу из `config.env`:
   - Код из `MAP_FEATURES` → `docs/features/`
   - Код из `MAP_ARCHITECTURE` → `docs/architecture/`
   - Код из `MAP_INFRASTRUCTURE` → `docs/infrastructure/`

6. **Проверь README в папках кода** (`src/`, `app/`, `lib/`):
   - Если README содержит архитектурную документацию → **предложи переместить в `docs/`**
   - Документация должна жить в `docs/`, не в коде

7. **НЕ создавай новые типы документов** (CHANGELOG, UPDATES, новые папки) без явного согласования

8. **Если проводился анализ** (проверка конфигурации, аудит, исследование) → обнови документацию с результатами

9. **Прочитай** затронутые документы

10. **Предложи конкретные изменения** в формате diff

11. **Примени** после подтверждения пользователя

### Важные правила

- **Не дублируй** — обновляй существующие документы, не создавай новые
- **README в коде → docs/** — документация из `src/**/*.md` должна быть в `docs/features/`
- **При сомнениях — спроси** — "Создать новый документ X или добавить в существующий Y?"
- **Результаты анализа** — если ты проводил проверку/анализ, зафиксируй результаты в доке
- **Pending updates** — это очередь отложенных обновлений между AI-сессиями

### Формат ответа

```
📋 Pending updates: 2 записи обработаны
⏰ Устаревшие документы: 3 обновлены

📄 Изменённые файлы:
  • src/hooks/useAuth.ts
  • src/components/LoginForm.tsx

📝 Затронутая документация:
  • docs/features/auth/README.md

⚠️ Требует внимания:
  • src/components/auth/README.md — переместить в docs/features/auth/

Предлагаемые изменения:
[diff]
```

---

## Структура документации

### Где должна быть документация

| Что | Где | НЕ здесь |
|-----|-----|----------|
| Архитектура компонентов | `docs/features/` | `src/**/README.md` |
| Схема данных, типы | `docs/architecture/` | `src/**/ARCHITECTURE.md` |
| API документация | `docs/architecture/` | `src/**/API.md` |
| Инструкции по деплою | `docs/infrastructure/` | корень проекта |
| История изменений | `docs/README.md` или ADR | отдельные CHANGELOG файлы |

### README.md в папках кода

Если видишь `README.md` в `src/`, `app/`, `lib/`, `packages/`:

1. **Проверь содержимое** — что там написано?
2. **Если это документация** (архитектура, API, как работает компонент):
   - → **Предложи переместить в `docs/features/[компонент]/`**
   - → Оставь в коде минимальный README со ссылкой на `docs/`
3. **Если это инструкции для разработчика** (как запустить тесты, линтер):
   - → Можно оставить в коде

### Не создавай без согласования

**Запрещено создавать без явного подтверждения:**

- ❌ Новые папки в `docs/` (кроме существующих: features, architecture, infrastructure, adr, plans, archive)
- ❌ Новые типы документов (CHANGELOG, UPDATES, DECISIONS, NOTES)
- ❌ Файлы с датами в названии (`*_2026-01-21.md`, `*_v2.md`)
- ❌ Дублирующие документы (если уже есть похожий — обнови его)

**Вместо этого:**

- ✅ Добавь информацию в существующий документ
- ✅ Создай ADR если это архитектурное решение
- ✅ Спроси: "Создать новый документ X или добавить в существующий Y?"

### Проверка перед созданием документа

Перед созданием нового файла в `docs/`:

1. **Поищи существующие** — `ls docs/` и подпапки
2. **Проверь README.md** — возможно информация должна быть там
3. **Проверь ADR** — если это решение, создай ADR
4. **Спроси пользователя** — если не уверен

### Результаты анализа

Если ты проводил:
- Проверку конфигурации
- Аудит безопасности
- Анализ производительности
- Исследование проблемы

**→ Обнови соответствующую документацию с результатами**, а не создавай отдельный файл.

---

# Правило: Запись в pending updates (для изменений без git commit)

## Когда записывать

Если ты изменяешь код **БЕЗ git commit** (например, работаешь в Cursor на удалённом сервере или просто редактируешь файлы):

1. После завершения изменений кода
2. Если изменения затрагивают бизнес-логику, архитектуру или API
3. Если пользователь **не** запустил команду `==` сразу после изменений

## Как записывать

### Формат записи

```
<timestamp>|<kind>|<ref>|<files>|<doc_hint>|<note>
```

**Поля:**
- `timestamp` — Unix timestamp: `$(date +%s)`
- `kind` — тип изменения: `code` (код), `infra` (инфраструктура), `schema` (схема БД)
- `ref` — источник: `cursor`, `manual`, `ai-edit`
- `files` — список файлов через **TAB** (`\t`): `file1.ts\tfile2.ts`
- `doc_hint` — куда обновить документацию: `docs/features/`, `docs/architecture/`, `docs/infrastructure/`
- `note` — краткое описание: `Cursor Agent` или `Added auth middleware`

### Пример команды

```bash
echo "$(date +%s)|code|cursor|src/auth/middleware.ts\tsrc/routes/api.ts|docs/features/auth/|Added JWT validation" >> .ai-docs-system/state/pending-updates.queue
```

### Определение doc_hint

Используй маппинг из `config.env`:

- **MAP_FEATURES** (например, `src/,app/,lib/`) → `docs/features/`
- **MAP_ARCHITECTURE** (например, `schema,models,types`) → `docs/architecture/`
- **MAP_INFRASTRUCTURE** (например, `deploy,docker,supabase`) → `docs/infrastructure/`

Если файл не соответствует маппингу → используй `docs/`

### Fallback для проблемных путей

Если в имени файла есть `|`, `\t` или `\n` — используй `.queue0` формат (NUL-separated):

```bash
{
  printf '%s\0%s\0%s\0' "$(date +%s)" "code" "cursor"
  printf '%s\0' "file with | pipe.ts" "another\tfile.ts"
  printf '\0%s\0%s\0\0' "docs/features/" "Cursor Agent"
} >> .ai-docs-system/state/pending-updates.queue0
```

## После записи

Напомни пользователю:

```
📝 Изменения записаны в pending updates.
💡 Запусти "==" чтобы обновить документацию.
```

## Когда НЕ записывать

- Пользователь явно попросил **только** изменить код (без документации)
- Изменения тривиальные (форматирование, опечатки)
- Уже запущена команда `==` в текущей сессии
- Изменения в файлах игнорируются (`IGNORE_DIRS` из `config.env`)

## Проверка перед записью

```bash
# Проверь что config существует
config=".ai-docs-system/config.env"
[[ ! -f "$config" ]] && echo "AI Docs System не установлен" && exit

# Проверь что запись включена
pending_write=$(grep "^PENDING_UPDATES_WRITE=" "$config" | cut -d'=' -f2)
[[ "$pending_write" == "off" ]] && echo "Запись в pending updates отключена" && exit

# Создай папку state если нет
mkdir -p .ai-docs-system/state
```

## Безопасность

- **НЕ** записывай sensitive данные в note (пароли, токены)
- **НЕ** записывай пути к файлам с секретами (`.env`, `credentials.json`)
- **НЕ** перезаписывай всю очередь — только добавляй (`>>`, не `>`)

---

