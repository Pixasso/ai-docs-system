# AI Docs System — Инструкции

> **Конфигурация:** `.ai-docs-system/config.env`  
> **Шаблоны:** `.ai-docs-system/templates/`

---

<!-- АВТОМАТИЧЕСКИ СОБРАНО ИЗ rules/ -->
<!-- Не редактируйте этот файл напрямую. Редактируйте rules/*.md и запустите install.sh update -->

## Doc-first (ОБЯЗАТЕЛЬНО)

Перед любым ответом или изменением кода:

1. **Прочитай** `docs/README.md` — это точка входа в документацию
2. **Найди** релевантные документы в `docs/` по теме запроса
3. **Следуй** существующей документации
4. **Если документация устарела** или противоречит коду — явно скажи об этом и предложи diff на обновление

> Документация — источник правды. Код должен соответствовать документации, не наоборот.

---

## Обновление документации

При изменении кода — **всегда** предлагай обновить соответствующую документацию.

### Маппинг изменений

| Что изменилось | Где обновить документацию |
|----------------|---------------------------|
| Бизнес-логика, компоненты, хуки, страницы | `docs/features/` |
| Схема данных, модели, типы, миграции | `docs/architecture/` |
| Инфраструктура, Docker, CI/CD, деплой | `docs/infrastructure/` |

### Как обновлять

1. Определи какие файлы изменились (`git diff --name-only`)
2. По маппингу (см. `config.env`) найди соответствующую документацию
3. Предложи конкретные изменения в формате diff
4. Примени после подтверждения

### Метаданные

При обновлении документа — обнови `Last verified` на текущую дату.

---

## Architecture Decision Records (ADR)

При принятии **архитектурного решения** — создавай ADR.

### Когда создавать ADR

- Выбор фреймворка, библиотеки или инструмента
- Решение о паттерне проектирования
- Компромиссы между вариантами
- Изменение существующей архитектуры
- Любое решение, влияющее на несколько частей системы

### Формат

```
docs/adr/NNNN-название-решения.md
```

Где `NNNN` — последовательный номер (0001, 0002, ...).

### Структура ADR

1. **Контекст** — какая проблема решается
2. **Решение** — что решили и почему
3. **Альтернативы** — что ещё рассматривали
4. **Последствия** — что изменится после решения

Используй шаблон из `.ai-docs-system/templates/adr.md`

---

## Шорткаты

### `==` — Обновить документацию

Когда пользователь вводит **"=="**, выполни:

1. **Найди изменения** — `git diff --name-only` (staged и unstaged)
2. **Определи затронутые документы** по маппингу из `config.env`:
   - Код из `MAP_FEATURES` → `docs/features/`
   - Код из `MAP_ARCHITECTURE` → `docs/architecture/`
   - Код из `MAP_INFRASTRUCTURE` → `docs/infrastructure/`
3. **Прочитай** затронутые документы
4. **Предложи конкретные изменения** в формате diff
5. **Примени** после подтверждения пользователя

### Формат ответа

```
📄 Изменённые файлы:
  • src/hooks/useAuth.ts
  • src/components/LoginForm.tsx

📝 Затронутая документация:
  • docs/features/auth/README.md

Предлагаемые изменения:
[diff]
```
