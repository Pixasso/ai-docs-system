## Шорткаты

### `==` — Обновить документацию

Когда пользователь вводит **"=="**, выполни:

1. **Проверь pending updates:**
   - Прочитай `PENDING_UPDATES_LOCAL` из конфига (если файл существует)
   - Прочитай `PENDING_UPDATES_SHARED` если задан
   - Также проверь `.queue0` файлы (fallback для путей с `|` или tab)
   - Формат: `timestamp|kind|ref|files_tab|doc_hint|note` (поля через `|`, файлы через TAB)
   - Если есть записи → покажи пользователю:
     ```
     📋 Необработанные изменения из предыдущих сессий:
     
     2026-01-22 15:30 (2 часа назад):
       • src/hooks/usePayments.ts
       • src/components/PaymentForm.tsx
       → Рекомендуется: docs/features/payments/
     
     Обработать их сейчас?
     ```
   - После обработки — **удали строку** из файла очереди

2. **Проверь устаревшие документы:**
   - Найди все файлы в `docs/` с полем `Last verified`
   - Если дата > `DOC_STALE_DAYS` дней назад → добавь в список
   - Если список > `DOC_STALE_MAX` → показывай топ + "ещё N документов"
   - Покажи пользователю:
     ```
     ⏰ Эти документы не проверялись >30 дней:
     
       • docs/infrastructure/DEPLOY.md — 219 дней назад
       • docs/features/auth/README.md — 52 дня назад
       (ещё 3 документа...)
     
     Пройтись по ним сейчас?
     ```
   - Если пользователь соглашается → прочитай, проверь актуальность, обнови `Last verified`

3. **Найди текущие изменения** — `git diff --name-only` (staged и unstaged)

4. **Проверь существующую документацию** — есть ли уже документы по этой теме в `docs/`

5. **Определи затронутые документы** по маппингу из `config.env`:
   - Код из `MAP_FEATURES` → `docs/features/`
   - Код из `MAP_ARCHITECTURE` → `docs/architecture/`
   - Код из `MAP_INFRASTRUCTURE` → `docs/infrastructure/`

6. **Проверь README в папках кода** (`src/`, `app/`, `lib/`):
   - Если README содержит архитектурную документацию → **предложи переместить в `docs/`**
   - Документация должна жить в `docs/`, не в коде

7. **НЕ создавай новые типы документов** (CHANGELOG, UPDATES, новые папки) без явного согласования

8. **Если проводился анализ** (проверка конфигурации, аудит, исследование) → обнови документацию с результатами

9. **Прочитай** затронутые документы

10. **Предложи конкретные изменения** в формате diff

11. **Примени** после подтверждения пользователя

### Важные правила

- **Не дублируй** — обновляй существующие документы, не создавай новые
- **README в коде → docs/** — документация из `src/**/*.md` должна быть в `docs/features/`
- **При сомнениях — спроси** — "Создать новый документ X или добавить в существующий Y?"
- **Результаты анализа** — если ты проводил проверку/анализ, зафиксируй результаты в доке
- **Pending updates** — это очередь отложенных обновлений между AI-сессиями

### Формат ответа

```
📋 Pending updates: 2 записи обработаны
⏰ Устаревшие документы: 3 обновлены

📄 Изменённые файлы:
  • src/hooks/useAuth.ts
  • src/components/LoginForm.tsx

📝 Затронутая документация:
  • docs/features/auth/README.md

⚠️ Требует внимания:
  • src/components/auth/README.md — переместить в docs/features/auth/

Предлагаемые изменения:
[diff]
```
