# AI Docs System

Универсальная модульная система автоматизации документации для проектов с AI-ассистентами.

## Что это?

- **Универсальный конфиг** — работает с любыми языками, фреймворками и структурами проекта
- **Модульные правила** — включай/выключай правила через конфиг
- **Мульти-AI** — поддержка Cursor, Copilot, Claude Code, Cline (и других)
- **Pre-commit hook** — напоминает обновить документацию при изменении кода
- **Doc-first подход** — AI сначала читает документацию, потом пишет код

## Установка

### macOS / Linux

```bash
# 1. Клонируй модуль
git clone https://github.com/Pixasso/ai-docs-system.git
cd ai-docs-system

# 2. Установи в свой проект
./install.sh /path/to/your/project

# Или в текущую директорию
./install.sh .
```

### Windows (PowerShell)

```powershell
# 1. Клонируй модуль
git clone https://github.com/Pixasso/ai-docs-system.git
cd ai-docs-system

# 2. Установи в свой проект
.\install.ps1 -Target "C:\path\to\your\project"

# Или в текущую директорию
.\install.ps1 -Target .
```

## Обновление

### Способ 1: Прямо из проекта (⭐ рекомендуется, v2.2+)

```bash
cd /path/to/your-project
.ai-docs-system/update.sh
```

Скрипт автоматически скачает последнюю версию и обновит систему.

### Способ 2: Из репозитория ai-docs-system

```bash
# macOS / Linux
cd ai-docs-system
git pull
./install.sh /path/to/your/project update

# Windows
cd ai-docs-system
git pull
.\install.ps1 -Target "C:\path\to\your\project" -Mode update
```

> **Режим `update`**: обновляет правила и хуки, мерджит новые ключи в `config.env` (не перезаписывает пользовательские значения)

## Что устанавливается

```
ваш-проект/
├── .ai-docs-system/           # Конфигурация системы
│   ├── config.env             # Настройки (пути, правила, адаптеры)
│   ├── instructions.md        # Собранные инструкции для AI
│   ├── rules/                 # Модульные правила
│   │   ├── doc-first.md
│   │   ├── update-docs.md
│   │   ├── adr.md
│   │   └── shortcuts.md
│   └── templates/             # Шаблоны документов
│       ├── adr.md
│       └── doc-header.md
├── .githooks/pre-commit       # Git hook для напоминаний
├── .cursorrules               # Адаптер для Cursor AI
└── docs/                      # Структура документации
```

## Конфигурация

Вся настройка — в одном файле: `.ai-docs-system/config.env`

```bash
# Какие папки считать кодом
CODE_DIRS=src,app,lib,packages,services

# Какие расширения — код
CODE_EXTS=ts,tsx,js,jsx,py,go,rs,java,php

# Где лежит документация
DOC_DIRS=docs

# Маппинг: тип кода → папка документации
MAP_FEATURES=src/,app/,lib/,components/
MAP_ARCHITECTURE=schema,models,database
MAP_INFRASTRUCTURE=deploy,docker,ci

# Какие правила включены
RULES_ENABLED=doc-first,update-docs,adr,shortcuts

# Какие адаптеры генерировать
ADAPTERS=cursor
```

После изменения конфига запустите:
```bash
# Из репозитория ai-docs-system:
cd /path/to/ai-docs-system
./install.sh /path/to/your-project update

# Или прямо из проекта (с v2.2+):
cd /path/to/your-project
.ai-docs-system/update.sh
```

## Модульные правила

Правила лежат в `.ai-docs-system/rules/`. Включайте/выключайте в `RULES_ENABLED`:

| Правило | Описание |
|---------|----------|
| `doc-first` | AI читает документацию перед ответом |
| `update-docs` | Предлагать обновить доку при изменении кода |
| `adr` | Создавать ADR при архитектурных решениях |
| `shortcuts` | Шорткат `==` для обновления документации |

### Свои правила

1. Создайте файл `.ai-docs-system/rules/my-rule.md`
2. Добавьте в `RULES_ENABLED`: `doc-first,update-docs,my-rule`
3. Запустите `./install.sh . update`

## Использование

### 1. Pre-commit Hook

При коммите кода без изменений в документации — напоминание:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  Напоминание: изменился код, но документация не обновлена
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Изменённые файлы:
  • src/hooks/usePayments.ts

💡 Рекомендуется обновить документацию:
   • Cursor Agent: введите '==' для автообновления
   • Или вручную обновите docs/
```

**Не блокирует коммит** — просто напоминание.

### 2. Cursor Agent

Запусти Cursor Agent (`Cmd+Shift+I`) и напиши:

- **`==`** — AI найдёт изменения и предложит обновления документации
- "Обнови документацию для изменений в [файл]"
- "Создай ADR для решения X"

### 3. Doc-first подход

AI автоматически:
1. Читает `docs/README.md` и релевантные документы
2. Следует существующей документации
3. Если дока устарела — предлагает diff на обновление

## Поддержка AI-ассистентов

По умолчанию генерируется только `.cursorrules`. Для других AI добавьте в конфиг:

```bash
ADAPTERS=cursor,copilot,claude,cline
```

| Адаптер | Файл |
|---------|------|
| cursor | `.cursorrules` |
| copilot | `.github/copilot-instructions.md` |
| claude | `CLAUDE.md` |
| cline | `.clinerules` |

Все адаптеры ссылаются на единый `instructions.md` — один источник правды.

## Структура docs/

```
docs/
├── README.md           # Точка входа, навигация
├── architecture/       # Архитектура системы
├── features/           # Функциональность по модулям
├── infrastructure/     # Серверы, деплой, DevOps
├── plans/              # Планы, идеи, roadmap
├── adr/                # Architecture Decision Records
└── archive/            # Устаревшие документы
```

## Лицензия

MIT — используй как хочешь.
