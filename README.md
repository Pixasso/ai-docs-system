# AI Docs System

Модульная система автоматизации документации для проектов с Cursor AI.

## Что это?

- **Pre-commit hook** — напоминает обновить документацию при изменении кода
- **Cursor Rules** — AI автоматически предлагает обновления документации
- **Структура docs/** — готовый шаблон организации документации
- **Doc-first подход** — AI сначала читает документацию, потом пишет код

## Установка

### macOS / Linux

```bash
# 1. Клонируй модуль
git clone https://github.com/Pixasso/ai-docs-system.git
cd ai-docs-system

# 2. Установи в свой проект
./install.sh /path/to/your/project

# Или в текущую директорию
./install.sh .
```

### Windows (PowerShell)

```powershell
# 1. Клонируй модуль
git clone https://github.com/Pixasso/ai-docs-system.git
cd ai-docs-system

# 2. Установи в свой проект
.\install.ps1 -Target "C:\path\to\your\project"

# Или в текущую директорию
.\install.ps1 -Target .
```

## Обновление

```bash
# macOS / Linux
cd ai-docs-system
git pull
./install.sh /path/to/your/project update

# Windows
cd ai-docs-system
git pull
.\install.ps1 -Target "C:\path\to\your\project" -Mode update
```

## Что устанавливается

| Файл/Папка | Назначение |
|------------|------------|
| `.githooks/pre-commit` | Hook для напоминаний |
| `.cursorrules` | Правила для Cursor AI (Doc-first блок) |
| `docs/` | Структура документации (только при install, не при update) |

## Использование

### 1. Pre-commit Hook

При коммите изменений в коде (без изменений в `docs/`) увидишь напоминание:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  Reminder: You changed code but no docs updated
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Changed files:
  • src/hooks/usePayments.ts

💡 Consider updating documentation:
   1. Use Cursor Agent (Cmd+Shift+I) and type "=="
   2. Or manually update relevant files in docs/
```

**Не блокирует коммит** — просто напоминание.

### 2. Cursor Agent

Запусти Cursor Agent (`Cmd+Shift+I`) и напиши:

- **"=="** — быстрый шорткат: AI найдёт и обновит релевантные документы
- "Обнови документацию с учётом изменений в [файл]"
- "Создай ADR для решения X"

### 3. Doc-first подход

AI автоматически:
1. Сначала читает `docs/README.md` и релевантные документы
2. Следует существующей документации
3. Если дока устарела — предлагает diff на обновление

## Структура docs/

```
docs/
├── README.md           # Точка входа, навигация, правила
├── architecture/       # Общая архитектура системы
├── features/           # Функциональность по модулям
├── infrastructure/     # Сервера, деплой, DevOps
├── plans/              # Планы, идеи, roadmap
├── adr/                # Architecture Decision Records
└── archive/            # Устаревшие документы
```

## Настройка под свой проект

### Папки с кодом

По умолчанию hook отслеживает `src/` и `services/`. Измени в `.githooks/pre-commit`:

```bash
# Было
grep -E "^(src/|services/)"

# Стало (пример для другого проекта)
grep -E "^(app/|lib/|packages/)"
```

### .cursorrules

Добавь свои правила после блока `# END ai-docs-system`:

```
# END ai-docs-system

## Мои правила
- Использовать TypeScript strict
- Отвечать на русском языке
```

## Миграция на npm (опционально)

Если захочешь распространять через npm:

```bash
# Добавь package.json и bin/cli.js
npm publish

# Использование
npx ai-docs-system init
npx ai-docs-system update
```

## Лицензия

MIT — используй как хочешь.
